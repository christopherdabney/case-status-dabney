# Copilot Instructions for Flask App

- Flask app initialized in current directory
- Virtual environment created with `uv venv .venv`
- Flask installed using `uv pip install flask`
- Main entry point: `app.py`
- See README.md for usage instructions
